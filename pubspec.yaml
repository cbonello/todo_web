name: test_indexdb
version: 1.0.0+1
description: A small project to test the sembast package with Flutter web.
publish_to: 'none' 

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  equatable: ^1.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^4.0.0
  sembast_web: ^1.1.0
  uuid: ^2.1.0

  cupertino_icons: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.2.0

flutter:
  uses-material-design: true
